{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe{% endblock %}

{% block content %}
    <h1>Modifier mon mt de passe</h1>

    <hr>
    <a href="{{ path('account') }}">retour a mon compte</a><br>
    <hr>
    {% if notif  %}
        <div class="alert alert-info">{{ notif }}</div>
    {% endif %}

    {{ form(form) }}

{% endblock %}
